---
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/Navbar.astro';
import Footer from '../../components/Footer.astro';
import ScrollUpButton from '../../components/ScrollUpButton.astro';

const siteUrl = 'https://widocznosc.ai';
const examples = [
  'najlepszy system CRM dla małej firmy',
  'ranking kont firmowych 2026',
  'jak wybrać platformę e-commerce B2B',
];
---

<Layout
  title="Analiza zapytań AI – query fan-out, źródła i cytowane domeny"
  description="Sprawdź, na jakie mniejsze pytania ChatGPT rozbija Twoją frazę i jakie źródła pobiera w tle. Bezpłatne narzędzie widocznosc.ai do analizy zapytań AI."
>
  <Navbar />

  <script
    type="application/ld+json"
    is:inline
    set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@graph': [
        {
          '@type': 'SoftwareApplication',
          name: 'Analiza zapytań AI',
          applicationCategory: 'BusinessApplication',
          operatingSystem: 'Web',
          url: `${siteUrl}/narzedzia/fanout`,
          description:
            'Bezpłatne narzędzie widocznosc.ai pokazujące, na jakie mniejsze pytania ChatGPT rozbija frazę i jakie domeny cytuje w odpowiedzi.',
          offers: {
            '@type': 'Offer',
            price: 0,
            priceCurrency: 'PLN',
          },
        },
        {
          '@type': 'HowTo',
          name: 'Jak sprawdzić query fan-out w ChatGPT',
          step: [
            {
              '@type': 'HowToStep',
              name: 'Wpisz zapytanie',
              text: 'Podaj frazę rankingową, porównawczą lub komercyjną do analizy.',
            },
            {
              '@type': 'HowToStep',
              name: 'Uruchom analizę',
              text: 'Kliknij przycisk, aby sprawdzić, jakie zapytania pomocnicze i źródła pojawią się w przebiegu wyszukiwania.',
            },
            {
              '@type': 'HowToStep',
              name: 'Przejrzyj domeny',
              text: 'Zobacz listę cytowanych domen i użyj jej do planowania treści pod widoczność w AI.',
            },
          ],
        },
      ],
    })}
  />

  <div class="flex-grow">
    <section class="tool-hero">
      <div class="content-container tool-hero-inner">
        <nav class="tool-breadcrumb" aria-label="Ścieżka nawigacji">
          <a href="/narzedzia" class="tool-breadcrumb-link">Narzędzia</a>
          <span class="tool-breadcrumb-sep" aria-hidden="true">/</span>
          <span class="tool-breadcrumb-current">Analiza zapytań AI</span>
        </nav>

        <header class="tool-hero-header">
          <span class="eyebrow tool-eyebrow">Narzędzie · bezpłatne</span>
          <h1 class="tool-hero-title display-xxl">
            Sprawdź, jak ChatGPT szuka informacji w&nbsp;tle
          </h1>
          <p class="tool-hero-copy">
            Wpisz interesującą Cię frazę, a my pokażemy, jak model rozbija ją na mniejsze pytania
            pomocnicze (tzw. Query Fan-out) oraz z jakich domen i źródeł ostatecznie czerpie
            wiedzę. To idealna wskazówka, pod jakie frazy optymalizować swoje teksty.
          </p>
        </header>

        <form id="fanout-form" class="fanout-form" novalidate>
          <label class="fanout-field">
            <span>Zapytanie</span>
            <input
              id="fanout-input"
              name="query"
              type="text"
              required
              maxlength="300"
              autocomplete="off"
              placeholder="np. najlepszy system CRM dla małej firmy"
            />
          </label>
          <button type="submit" class="btn-primary fanout-submit">Zobacz rozbicie zapytania</button>
          <div class="fanout-examples" aria-label="Przykładowe zapytania">
            {examples.map((ex) => (
              <button type="button" class="fanout-example" data-query={ex} aria-pressed="false">
                {ex}
              </button>
            ))}
          </div>
          <div class="fanout-form-meta">
            <span>Maksymalnie 300 znaków</span>
            <span id="fanout-counter">0/300</span>
          </div>
        </form>

        <div id="fanout-results" class="fanout-results hidden" aria-live="polite"></div>

        <p class="fanout-note">
          Wynik prezentuje dane dla tej konkretnej sesji wyszukiwania. Pamiętaj, że odpowiedzi
          modeli AI są generowane dynamicznie i mogą się nieznacznie różnić w czasie.
        </p>
      </div>
    </section>
  </div>

  <Footer />
  <ScrollUpButton />
</Layout>

<style>
  .tool-hero {
    position: relative;
    overflow: hidden;
    padding: clamp(3.5rem, 8vw, 7rem) 0 clamp(4rem, 8vw, 7.5rem);
    background:
      radial-gradient(circle at 20% 12%, var(--accent-blue-soft), transparent 34rem),
      linear-gradient(180deg, var(--bg-surface-1), var(--bg-canvas));
  }

  .tool-hero-inner {
    position: relative;
    z-index: 1;
  }

  .tool-breadcrumb {
    display: flex;
    justify-content: flex-start;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 2rem;
    color: var(--ink-muted);
    font-size: 0.86rem;
  }

  .tool-breadcrumb-link {
    color: inherit;
    text-decoration: none;
  }

  .tool-breadcrumb-link:hover {
    color: var(--accent-blue);
  }

  .tool-breadcrumb-current {
    color: var(--ink);
    font-weight: 500;
  }

  .tool-eyebrow {
    margin-bottom: 1rem;
  }

  .tool-hero-title {
    max-width: 980px;
    margin: 0;
    font-size: clamp(36px, 4.1vw, 56px);
    line-height: 0.96;
    letter-spacing: -0.045em;
    color: var(--ink);
  }

  .tool-hero-accent {
    color: var(--accent-blue);
  }

  .tool-hero-copy {
    max-width: 760px;
    margin: 1.4rem 0 0;
    color: var(--ink-muted);
    font-size: clamp(1.05rem, 1.6vw, 1.3rem);
    line-height: 1.55;
  }

  .fanout-form {
    max-width: 820px;
    margin: 2.75rem auto 0;
    padding: clamp(1rem, 2.4vw, 1.5rem);
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 0.85rem;
    border: 1px solid var(--hairline);
    border-radius: 1.25rem;
    background: var(--bg-surface-1);
    box-shadow: 0 24px 70px rgba(15, 23, 42, 0.08);
  }

  .fanout-field { display: flex; flex-direction: column; gap: 0.45rem; text-align: left; font-weight: 500; }
  .fanout-field input {
    width: 100%;
    min-height: 3.35rem;
    padding: 0.9rem 1rem;
    border-radius: 0.85rem;
    border: 1px solid var(--hairline);
    background: var(--bg-canvas);
    color: var(--ink);
    font-size: 1rem;
  }

  .fanout-field input:focus {
    outline: 2px solid var(--accent-blue-soft);
    outline-offset: 2px;
  }

  .fanout-submit {
    align-self: end;
    min-height: 3.35rem;
    white-space: nowrap;
  }

  .fanout-examples {
    grid-column: 1 / -1;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 0.1rem;
  }

  .fanout-example {
    font-size: 0.86rem;
    padding: 0.42rem 0.78rem;
    border-radius: 999px;
    cursor: pointer;
    border: 1px solid var(--hairline);
    background: var(--accent-blue-soft);
    color: var(--ink);
  }

  .fanout-example:hover,
  .fanout-example[aria-pressed='true'] {
    border-color: var(--accent-blue);
    color: var(--accent-blue);
  }

  .fanout-form-meta {
    grid-column: 1 / -1;
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    color: var(--ink-faint);
    font-size: 0.78rem;
    line-height: 1.4;
  }

  .fanout-results {
    max-width: 1080px;
    margin: 2.75rem auto 0;
    text-align: left;
    display: grid;
    gap: 1rem;
  }
  .fanout-results.hidden { display: none; }

  :global(.fanout-panel) {
    border: 1px solid var(--hairline);
    border-radius: 1.15rem;
    padding: clamp(1.15rem, 2.3vw, 1.75rem);
    background: var(--bg-surface-1);
    text-align: left;
  }

  :global(.fanout-panel h2) {
    margin: 0;
    color: var(--ink);
    font-family: var(--font-display);
    font-size: 23px !important;
    line-height: 1.16 !important;
    letter-spacing: -0.018em;
  }

  :global(.fanout-result-meta) {
    margin-bottom: 0.7rem;
  }

  :global(.fanout-panel--empty) {
    max-width: 780px;
    margin: 0 auto;
  }

  :global(.fanout-panel-copy) {
    margin: 0.45rem 0 1.15rem;
    color: var(--ink-muted);
    font-size: 0.96rem;
    line-height: 1.55;
  }

  :global(.fanout-grid) {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(18rem, 0.68fr);
    gap: 1rem;
  }

  :global(.fanout-answer) {
    color: var(--ink);
    font-size: 1rem;
    line-height: 1.65;
  }

  :global(.fanout-list) { list-style: none; margin: 0; padding: 0; display: grid; gap: 0.65rem; }
  :global(.fanout-list li) {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.7rem;
    align-items: start;
    padding: 0.78rem 0.9rem;
    border-radius: 0.75rem;
    background: var(--accent-blue-soft);
    color: var(--ink);
    font-size: 0.94rem;
    line-height: 1.45;
  }

  :global(.fanout-index) {
    display: inline-flex;
    width: 1.75rem;
    height: 1.75rem;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    background: var(--bg-surface-2);
    color: var(--accent-blue);
    font-size: 0.78rem;
    font-weight: 500;
  }

  :global(.domain-list) {
    display: grid;
    gap: 0.65rem;
  }

  :global(.domain-card) {
    padding: 0.85rem 0.95rem;
    border: 1px solid var(--hairline-soft);
    border-radius: 0.85rem;
    background: var(--bg-canvas);
  }

  :global(.domain-row) { display: flex; align-items: center; gap: 0.75rem; }
  :global(.domain-name) { font-weight: 600; color: var(--ink); }
  :global(.domain-count) {
    margin-left: auto;
    flex: 0 0 auto;
    font-size: 0.78rem;
    font-weight: 500;
    padding: 0.2rem 0.62rem;
    border-radius: 999px;
    background: var(--accent-blue-soft);
    color: var(--accent-blue);
  }

  :global(.domain-urls) { display: grid; gap: 0.32rem; margin: 0.62rem 0 0; }
  :global(.domain-urls a) {
    color: var(--ink-muted);
    font-size: 0.82rem;
    line-height: 1.35;
    word-break: break-all;
  }

  :global(.source-grid) {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.55rem;
  }

  :global(.source-link) {
    display: block;
    padding: 0.72rem 0.82rem;
    border: 1px solid var(--hairline-soft);
    border-radius: 0.75rem;
    background: var(--bg-canvas);
    color: var(--ink-muted);
    font-size: 0.84rem;
    line-height: 1.35;
    word-break: break-all;
  }

  :global(.fanout-loading-card) {
    width: min(100%, 820px);
    margin-inline: auto;
    background: var(--bg-surface-1);
    border: 1px solid var(--hairline-soft);
    border-radius: var(--r-xl);
    padding: clamp(22px, 3vw, 32px);
    box-shadow: 0 20px 56px rgb(15 23 42 / 0.06);
  }

  :global(.fanout-loading-header) {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 24px;
    margin-bottom: 22px;
  }

  :global(.fanout-loading-kicker) {
    color: var(--accent-blue);
    margin-bottom: 8px;
  }

  :global(.fanout-loading-title) {
    font-family: var(--font-display);
    font-size: clamp(26px, 3.2vw, 40px);
    line-height: 1.05;
    font-weight: 500;
    color: var(--ink);
    letter-spacing: -0.031em;
    margin: 0;
  }

  :global(.fanout-loading-copy) {
    color: var(--ink-muted);
    font-size: 14px;
    line-height: 1.55;
    margin: 10px 0 0;
    max-width: 560px;
  }

  :global(.fanout-loading-orb) {
    width: 42px;
    height: 42px;
    flex: none;
    border-radius: 50%;
    border: 2px solid var(--accent-blue-soft);
    border-top-color: var(--accent-blue);
    animation: spin 0.8s linear infinite;
  }

  @keyframes spin {
    to { transform: rotate(360deg); }
  }

  :global(.fanout-loading-track) {
    position: relative;
    height: 8px;
    overflow: hidden;
    border-radius: var(--r-pill);
    background: var(--bg-surface-2);
    margin: 20px 0 22px;
  }

  :global(.fanout-loading-bar) {
    position: absolute;
    inset: 0 auto 0 0;
    width: 42%;
    border-radius: inherit;
    background: var(--accent-blue);
    animation: fanout-progress 1.45s ease-in-out infinite;
  }

  @keyframes fanout-progress {
    0% { transform: translateX(-105%); }
    55% { transform: translateX(95%); }
    100% { transform: translateX(250%); }
  }

  :global(.fanout-loading-steps) {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
  }

  :global(.fanout-loading-step) {
    min-width: 0;
    padding: 12px 13px;
    border-radius: var(--r-md);
    background: var(--bg-canvas);
    border: 1px solid var(--hairline-soft);
  }

  :global(.fanout-loading-step-name) {
    color: var(--ink);
    font-size: 13px;
    font-weight: 600;
    line-height: 1.2;
  }

  :global(.fanout-loading-step-status) {
    color: var(--ink-muted);
    font-size: 11.5px;
    line-height: 1.25;
    margin-top: 5px;
  }

  :global(.fanout-error) { padding: 1rem 1.25rem; border-radius: 0.75rem; background: var(--err-bg); color: var(--err-fg); }
  :global(.fanout-cta) { margin-top: 0.75rem; }
  :global(.fanout-note),
  .fanout-note { margin-top: 1.25rem; color: var(--ink-faint); text-align: center; font-size: 0.8rem; }

  :global(.tool-next-steps) {
    border: 1px solid var(--hairline);
    border-radius: var(--r-xl);
    padding: clamp(1.15rem, 2.3vw, 1.75rem);
    background: var(--bg-surface-1);
  }

  :global(.tool-next-steps h2) {
    margin: 0 0 1rem;
    color: var(--ink);
    font-size: 23px !important;
    line-height: 1.16;
    letter-spacing: -0.018em;
  }

  :global(.next-grid) {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.75rem;
  }

  :global(.next-card) {
    display: block;
    min-width: 0;
    padding: 1rem;
    border: 1px solid var(--hairline-soft);
    border-radius: var(--r-md);
    background: var(--bg-canvas);
    color: inherit;
    text-decoration: none;
  }

  :global(.next-card:hover) {
    border-color: var(--accent-blue);
  }

  :global(.next-card h3) {
    margin: 0.65rem 0 0;
    color: var(--ink);
    font-family: var(--font-display);
    font-size: clamp(18px, 1.5vw, 22px);
    font-weight: 500;
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  @media (max-width: 780px) {
    .tool-hero {
      padding-top: 2.8rem;
    }

    .fanout-form,
    :global(.fanout-grid),
    :global(.source-grid),
    :global(.next-grid) {
      grid-template-columns: 1fr;
    }

    .fanout-submit {
      width: 100%;
      justify-content: center;
    }

    :global(.fanout-loading-header) {
      gap: 16px;
    }

    :global(.fanout-loading-orb) {
      width: 34px;
      height: 34px;
    }

    :global(.fanout-loading-steps) {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  const initFanout = () => {
    const form = document.getElementById('fanout-form');
    const input = document.getElementById('fanout-input');
    const results = document.getElementById('fanout-results');
    if (!form || !results || !input) return;

    const submit = form.querySelector('button[type="submit"]');
    const counter = document.getElementById('fanout-counter');
    const exampleButtons = Array.from(form.querySelectorAll('.fanout-example'));

    function escapeHtml(value) {
      return String(value ?? '')
        .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
    }

    function countLabel(count, one, few, many) {
      const value = Number(count);
      if (value === 1) return one;
      if (value > 1 && value < 5) return few;
      return many;
    }

    function cleanUrlLabel(url) {
      try {
        const parsed = new URL(String(url));
        return `${parsed.hostname.replace(/^www\./i, '')}${parsed.pathname === '/' ? '' : parsed.pathname}`;
      } catch {
        return String(url);
      }
    }

    function setLoading(isLoading) {
      if (!submit) return;
      submit.disabled = isLoading;
      submit.setAttribute('aria-busy', isLoading ? 'true' : 'false');
    }

    function renderError(message) {
      results.classList.remove('hidden');
      results.innerHTML = `<div class="fanout-error">${escapeHtml(message)}</div>`;
    }

    function renderNextSteps() {
      return `
        <section class="tool-next-steps" aria-labelledby="fanout-next-title">
          <h2 id="fanout-next-title" class="display-md">Co dalej?</h2>
          <div class="next-grid">
            <a class="next-card" href="/narzedzia/brand-check">
              <span class="eyebrow">Marka w AI</span>
              <h3>Sprawdź, co modele wiedzą o Twojej firmie</h3>
            </a>
            <a class="next-card" href="/geo/query-fan-out">
              <span class="eyebrow">Czytaj</span>
              <h3>Jak działa Query Fan-out w ChatGPT</h3>
            </a>
            <a class="next-card" href="/kontakt?type=audyt-ai">
              <span class="eyebrow">Audyt AI</span>
              <h3>Zamów pełny audyt widoczności w AI</h3>
            </a>
          </div>
        </section>`;
    }

    function renderResults(data) {
      if (data.status === 'no-search') {
        results.innerHTML = `
          <div class="fanout-panel fanout-panel--empty">
            <div class="head-meta fanout-result-meta">
              <span class="section-index">F/01</span>
              <span class="eyebrow">Wynik</span>
            </div>
            <h2>Brak zapytań wyszukiwania</h2>
            <p class="fanout-panel-copy">W tym przebiegu model nie ujawnił osobnego wyszukiwania w sieci. Spróbuj frazy rankingowej, porównawczej albo aktualnego pytania branżowego.</p>
          </div>
          ${renderNextSteps()}`;
        return;
      }

      const fanouts = (data.fanoutQueries || [])
        .map((q, index) => `<li><span class="fanout-index">${String(index + 1).padStart(2, '0')}</span><span>${escapeHtml(q)}</span></li>`)
        .join('');
      const domains = (data.citedDomains || [])
        .map((d) => {
          const count = Number(d.count) || 0;
          return `
          <div class="domain-card">
            <div class="domain-row">
              <span class="domain-name">${escapeHtml(d.domain)}</span>
              <span class="domain-count">${escapeHtml(String(count))} ${countLabel(count, 'cytowanie', 'cytowania', 'cytowań')}</span>
            </div>
            <div class="domain-urls">
              ${(d.urls || [])
                .filter((u) => /^https?:\/\//i.test(String(u)))
                .slice(0, 4)
                .map((u) => `<a href="${escapeHtml(u)}" target="_blank" rel="nofollow noopener">${escapeHtml(cleanUrlLabel(u))}</a>`)
                .join('')}
            </div>
          </div>`;
        })
        .join('');
      const remaining = data.usage?.remaining;
      const ctaHtml = Number.isFinite(remaining) && remaining <= 0
        ? `<p class="fanout-cta"><a class="btn-primary" href="/kontakt">Chcesz pełny audyt widoczności w AI? Napisz do nas</a></p>`
        : Number.isFinite(remaining)
          ? `<p class="fanout-note">Pozostały Ci dziś ${escapeHtml(String(remaining))} zapytania.</p>`
          : '';

      results.innerHTML = `
        <section class="fanout-panel">
          <div class="head-meta fanout-result-meta">
            <span class="section-index">F/01</span>
            <span class="eyebrow">Zapytania</span>
          </div>
          <h2>Zapytania wyszukiwania (${escapeHtml(String((data.fanoutQueries || []).length))})</h2>
          <p class="fanout-panel-copy">Frazy ujawnione przez model w tym przebiegu dla: „${escapeHtml(data.query)}”.</p>
          <ul class="fanout-list">${fanouts || '<li>brak</li>'}</ul>
        </section>
        <section class="fanout-panel">
          <div class="head-meta fanout-result-meta">
            <span class="section-index">F/02</span>
            <span class="eyebrow">Źródła</span>
          </div>
          <h2>Cytowane domeny</h2>
          <p class="fanout-panel-copy">Domeny, które trafiły do finalnej odpowiedzi modelu.</p>
          <div class="domain-list">${domains || '<p class="fanout-panel-copy">Brak cytowań w odpowiedzi.</p>'}</div>
        </section>
        ${ctaHtml}
        ${renderNextSteps()}
      `;
    }

    function updateCounter() {
      if (counter) counter.textContent = `${String(input.value || '').length}/300`;
    }

    function updateExampleState() {
      const value = String(input.value || '').trim();
      exampleButtons.forEach((btn) => {
        const active = value === btn.getAttribute('data-query');
        btn.setAttribute('aria-pressed', active ? 'true' : 'false');
      });
    }

    input.addEventListener('input', () => {
      updateCounter();
      updateExampleState();
    });

    exampleButtons.forEach((btn) => {
      btn.addEventListener('click', (event) => {
        event.preventDefault();
        event.stopPropagation();
        input.value = btn.getAttribute('data-query') || '';
        updateCounter();
        updateExampleState();
        input.focus();
      });
    });

    updateCounter();
    updateExampleState();

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const query = String(input.value || '').trim();
      if (query.length < 3) { renderError('Podaj zapytanie (min. 3 znaki).'); return; }

      setLoading(true);
      results.classList.remove('hidden');
      results.innerHTML = `
        <div class="fanout-loading-card">
          <div class="fanout-loading-header">
            <div>
              <div class="eyebrow fanout-loading-kicker">Analiza w toku</div>
              <h2 class="fanout-loading-title">Sprawdzamy zapytanie</h2>
              <p class="fanout-loading-copy">
                Model przygotowuje odpowiedź, uruchamia wyszukiwanie i zwraca źródła dla: „${escapeHtml(query)}”.
              </p>
            </div>
            <div class="fanout-loading-orb" aria-hidden="true"></div>
          </div>
          <div class="fanout-loading-track" aria-hidden="true">
            <div class="fanout-loading-bar"></div>
          </div>
          <div class="fanout-loading-steps">
            <div class="fanout-loading-step">
              <div class="fanout-loading-step-name">Zapytania</div>
              <div class="fanout-loading-step-status">oczekiwanie na wynik</div>
            </div>
            <div class="fanout-loading-step">
              <div class="fanout-loading-step-name">Źródła</div>
              <div class="fanout-loading-step-status">zbieranie cytowań</div>
            </div>
            <div class="fanout-loading-step">
              <div class="fanout-loading-step-name">Domeny</div>
              <div class="fanout-loading-step-status">agregacja widoczności</div>
            </div>
          </div>
        </div>`;

      try {
        const response = await fetch('/api/tools/fanout', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ query }),
        });
        const body = await response.json();
        if (!response.ok) throw new Error(body.error || `HTTP ${response.status}`);
        renderResults(body);
      } catch (error) {
        renderError(error instanceof Error ? error.message : 'Nie udało się wykonać zapytania.');
      } finally {
        setLoading(false);
      }
    });
  };

  document.addEventListener('astro:page-load', initFanout);
</script>
